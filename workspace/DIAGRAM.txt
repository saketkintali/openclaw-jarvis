     ╔══════════════════════════════════════════════════════════════════════════╗
     ║                        YOUR WHATSAPP                                    ║
     ╚══════════════════════════╤═══════════════════════════════════════════════╝
                                │
                     ┌──────────▼──────────┐
                     │   OpenClaw Gateway   │  port 18789
                     │   (WhatsApp bridge)  │
                     └──────────┬──────────┘
                                │
                ┌───────────────┴───────────────┐
          Voice message                    Text message
                │                               │
                ▼                               │
  ┌─────────────────────────┐                   │
  │  audio-transcribe hook  │                   │
  │  → check_audio.py       │                   │
  │    transcribe_audio()   │                   │
  │    → transcribe.py      │                   │
  │    (faster-whisper,     │                   │
  │     tiny model, CPU)    │                   │
  │    → transcript text    │                   │
  └──────────┬──────────────┘                   │
             │ get_ai_response(transcript)       │
             └─────────────────┬─────────────────┘
                               │
╔══════════════════════════════▼═══════════════════════════════════════════════╗
║            CLAUDE NATIVE AGENT  (claude-sonnet-4-6)                         ║
║                                                                              ║
║  System prompt: workspace/*.md files (SOUL.md, AGENTS.md, IDENTITY.md ...)  ║
║  Jarvis persona is defined there — no extra config needed.                  ║
║                                                                              ║
║  Claude reads the message, decides what tool (if any) to call, calls it,   ║
║  gets the result, and composes a Jarvis-style reply.                         ║
╚══╤═════════╤══════════════╤═══════════════╤══════════════╤═══════════════════╝
   │         │              │               │              │
   ▼         ▼              ▼               ▼              ▼
JARVIS-TOOLS MCP SERVER (mcp_server.py — stdio, started by OpenClaw)
   │         │              │               │              │
┌──┴──┐  ┌───┴───┐  ┌───────┴──────┐  ┌────┴─────┐  ┌────┴──────┐
│get_ │  │get_   │  │get_movies()  │  │find_     │  │set_/get_  │
│weath│  │time() │  │→ TMDB API    │  │nearby()  │  │reminder() │
│er() │  │→ world│  │  search by   │  │→ Overpass│  │→ local    │
│→ op-│  │  time │  │  actor /     │  │  API     │  │  remind-  │
│ enm-│  │  REST │  │  director,   │  │  (Open-  │  │  ers.json │
│ eteo│  │  API  │  │  returns     │  │  Street- │  │           │
│ API │  │       │  │  titles +    │  │  Map)    │  │           │
│     │  │       │  │  budget/box  │  │  up to 3 │  │           │
│curr.│  │       │  │  office      │  │  results │  │           │
│+ hr.│  │       │  │              │  │  + dist. │  │           │
└─────┘  └───────┘  └──────────────┘  └──────────┘  └───────────┘

                    ZAPIER MCP SERVER (config/mcporter.json)
                              │              │
                        ┌─────┴────┐   ┌─────┴────────┐
                        │ Gmail    │   │ Google       │
                        │ Find     │   │ Calendar     │
                        │ Email    │   │ Find Events  │
                        │ tool     │   │ Quick Add    │
                        └──────────┘   └──────────────┘

            GENERAL / NUTRITION (no tool call)
            Claude answers directly from training knowledge.


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  REPLY PATH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Text message reply:
    Claude → response text → OpenClaw → WhatsApp

  Voice message reply:
    Claude → response text → check_audio.py
      → send_whatsapp_audio() → edge-tts → OGG file → WhatsApp
      → (fallback to text if TTS fails)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BACKGROUND HEARTBEAT  (Windows Task Scheduler, every ~30 min)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  heartbeat.py runs
        │
        └─► check_due_reminders()
              reads reminders.json
              for each reminder where remind_at ≤ now:
                → send_whatsapp("⏰ Reminder: [task]")
                → remove from reminders.json


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  KEY FILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  jarvis.py            ← shared utility: fetch functions, TTS, gateway API, constants
  mcp_server.py        ← FastMCP stdio server; wraps jarvis.py tools for Claude
  check_audio.py       ← voice pipeline: lock, state, transcribe, call gateway
  heartbeat.py         ← fires due reminders (Task Scheduler, every ~30 min)
  transcribe.py        ← faster-whisper wrapper (called as subprocess)
  config/mcporter.json ← Zapier MCP + local jarvis-tools MCP endpoints
  reminders.json       ← active reminders [{task, remind_at, id}]
  audio_state.json     ← tracks which audio files have already been processed
  workspace/*.md       ← Jarvis persona / system prompt (SOUL.md, AGENTS.md, etc.)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  INTENT ROUTING  (handled natively by Claude — no classify_intent() needed)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Weather       → Claude calls get_weather(location)
  Time          → Claude calls get_time(location)
  Movies        → Claude calls get_movies(query)
  Nearby        → Claude calls find_nearby(query, location)
  Reminder set  → Claude calls set_reminder(task, remind_at)
  Reminder check→ Claude calls get_due_reminders()
  Email         → Claude calls Zapier find_email tool
  Calendar find → Claude calls Zapier find_calendar_events tool
  Calendar add  → Claude calls Zapier quick_add_event tool
  Nutrition     → Claude answers from knowledge (no tool call)
  General       → Claude answers from knowledge (no tool call)
