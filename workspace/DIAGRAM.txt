     ╔══════════════════════════════════════════════════════════════════════════╗
     ║                        YOUR WHATSAPP (Saket)                            ║
     ╚══════════════════════════════╤═══════════════════════════════════════════╝
                                    │
                         ┌──────────▼──────────┐
                         │   Twilio Webhook     │  cloud.twilio.com
                         │   HTTP POST to       │  routes WA messages
                         │   your server        │  to your code
                         └──────────┬──────────┘
                                    │
                    ┌───────────────┴───────────────┐
              Voice (OGG)                       Text message
                    │                               │
                    ▼                               ▼
         ┌─────────────────────┐       ┌─────────────────────┐
         │   check_audio.py    │       │   check_text.py      │
         │                     │       │                      │
         │  transcribe_audio() │       │  reads message body  │
         │       calls         │       │  cleans + lowercases │
         │   transcribe.py     │       │  → clean variable    │
         │  (faster-whisper    │       └──────────┬──────────┘
         │   tiny model, CPU)  │                  │
         │  → transcript str   │                  │
         └──────────┬──────────┘                  │
                    └──────────────┬──────────────┘
                                   │ transcript / clean
                                   ▼
╔══════════════════════════════════════════════════════════════════════════╗
║                         INTENT ROUTER                                   ║
║          classify_intent() — Groq llama-3.1-8b-instant                  ║
║          (temperature=0, max_tokens=50, direct to api.groq.com)         ║
║          returns JSON: {"intent": "...", "location": "City or null"}    ║
║          location=null → use DEFAULT_LOCATION for weather/time          ║
║                                                                         ║
║  "reminder"        →  setting or asking about a reminder                ║
║  "weather"         →  weather, jacket, outdoor activity, going out now  ║
║  "time"            →  current time in a specific location               ║
║  "email"           →  checking or reading inbox / gmail                 ║
║  "calendar_find"   →  what's on schedule, meetings, availability        ║
║  "calendar_create" →  add / book / create a new calendar event          ║
║  "calendar_delete" →  remove / cancel / delete an existing event        ║
║  "general"         →  anything else                                     ║
║                                                                         ║
║  ⚠ Groq fails → _keyword_fallback() (original is_X_question() chain)   ║
╚═╤════╤══════╤════╤═════════╤══════════╤═════════════╤══════════╤════════╝
  │    │      │    │         │          │             │          │
  ▼    ▼      ▼    ▼         ▼          ▼             ▼          ▼
┌────────────────────────────────────────────────────────────────────────────────────────┐
│ REMINDER   WEATHER    TIME      EMAIL     CAL FIND  CAL CREATE  CAL DELETE  GENERAL   │
│                                                                                        │
│ parse_     location   location  build     build     build       NL instruct query      │
│ reminder_  from       from      instr +   instr +   instr +     to Zapier:  to         │
│ groq()     classify_  classify_ query +   date      date        "do not     Groq 70b   │
│ (8b,       intent()   intent()  date ctx  window    window      ask follow- direct     │
│  temp=0,   │          │         │         (SOD/EOD  (natural    ups, delete            │
│  JSON)     ▼          ▼         ▼         from      language    now"                   │
│            fetch_     fetch_    fetch_    query)    query)      │                      │
│ ▼          weather()  world-    gmail_    │          │          if followUp:           │
│ save to    open-meteo time      zapier()  fetch_    create_     auto-find →            │
│ remind.    REST API   REST API  │         cal_      cal_event_  parse title            │
│ json       curr +     │         each      zapier()  zapier()    + time →              │
│            hourly     │         line as   Zapier    Zapier MCP  retry delete           │
│ ▼          precip.    │         own       MCP       (HTTP SSE)  │                      │
│ Groq 70b   │          │         bubble    (HTTP     Quick Add   ▼                      │
│ confirms   ▼          │         ──────    SSE)      Event tool  delete_cal_            │
│ "saved,    Groq 70b   │         no Groq   gcal_     NL→Google   event_                 │
│  sir"      answers    │         filter    find_     │           zapier()               │
│            specific   │         ──────    events    │           Zapier MCP             │
│            question   │         sent as   tool      │           (HTTP SSE)             │
│            ("jacket?" │         bubbles   │          │           │                      │
│             "cold?")  │                   ▼          │           │                      │
│                       │                   Groq 70b  └───────────┘                      │
│                       │                   answers    Groq 70b confirms action           │
│                       └───────────────────┘                                            │
└────────────────────────────────────────────────────────────────────────────────────────┘
         │                    │                    │
         │ Groq 70b called    │ raw string         │ split bubbles
         │ inside handler     │ (no Groq)          │ (no Groq)
         │ (weather/cal/      │                    │
         │  reminder/general) │                    │
         └────────────────────┴────────────────────┘
                              │ final response
                              ▼
              ┌───────────────┴──────────────┐
         Voice input?                    Text input?
              │                               │
              ▼                               ▼
       edge-tts (local)              plain text / bubbles
       text → OGG audio              sent via openclaw.cmd
              │                               │
              └───────────────┬───────────────┘
                              │
                   ┌──────────▼──────────┐
                   │   Twilio sends      │
                   │   reply back        │
                   └──────────┬──────────┘
                              │
╔═════════════════════════════▼════════════════════════════════════════╗
║            YOUR WHATSAPP — audio reply or text reply                ║
╚══════════════════════════════════════════════════════════════════════╝


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GROQ USAGE SUMMARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  classify_intent()     llama-3.1-8b-instant  temp=0  max_tokens=50
                        → returns JSON {intent, location}
                        → routing + location extraction in one call
                        → location=null uses DEFAULT_LOCATION

  get_groq_response()   llama-3.3-70b-versatile  temp=0.7  max_tokens=150
                        → called from within each handler (not a pipeline step)
                        → prompt = "User asked: [query]. Data: [raw]. Answer
                          their specific question in one sentence as Jarvis."
                        → TIME and EMAIL bypass this — raw data sent directly

  groq_proxy :11435     NOT a Groq proxy — returns empty "" to every request
                        → silences the OpenClaw main agent so it doesn't send
                          its own reply to WhatsApp on top of ours


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  BACKGROUND HEARTBEAT  (Windows Task Scheduler, every ~30 min)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  check_amazon.py runs
        │
        └─► check_due_reminders()
              reads reminders.json
              for each reminder where remind_at ≤ now:
                → send_whatsapp("⏰ Reminder: [task]")
                → remove from reminders.json


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  KEY FILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  check_audio.py      ← main voice pipeline + all fetch/handler functions
  check_text.py       ← text pipeline, imports all functions from above
  transcribe.py       ← faster-whisper wrapper (called as subprocess)
  groq_proxy.py       ← OpenClaw silencer on :11435, returns empty response
  check_amazon.py     ← heartbeat: fires due reminders
  config/mcporter.json← Zapier MCP endpoint URL + auth token
  reminders.json      ← active reminders [{task, remind_at, id}]
  audio_state.json    ← tracks which OGG files already processed
